[project]
name = "embed"
version = "1.0.0"
requires-python = ">=3.14"
dependencies = [
    "boto3",
    "openai",
    "qdrant-client",
    "tiktoken",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.10",
]

[tool.uv]
package = false
