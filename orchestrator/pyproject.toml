[project]
name = "orchestrator"
version = "1.0.0"
requires-python = ">=3.13"
dependencies = [
    "boto3==1.40.15",
    "requests==2.32.4",
]

[dependency-groups]
dev = [
    "moto[ec2,s3]>=5.0",
    "pytest>=8.0",
    "ruff>=0.10",
]

[tool.uv]
package = false
