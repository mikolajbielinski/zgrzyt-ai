[project]
name = "backend"
version = "1.0.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.0",
    "openai>=1.82.0",
    "qdrant-client>=1.9.0",
    "uvicorn>=0.34.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.10",
]

[tool.uv]
package = false
